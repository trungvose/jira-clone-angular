query FindProjectBySlug($slug: String!) {
  findProjectBySlug(slug: $slug) {
    id
    name
    slug
    description
    category
    users {
      id
      fullName
      avatarUrl
    }
    lanes {
      id
      title
      issues {
        id
        name
        title
        isActive
        createdAt
        updatedAt
        type
        status
        priority
        tags {
          id
          text
          styles {
            color
            backgroundColor
          }
          description
        }
        main {
          id
          avatarUrl
          fullName
        }
      }
    }
    createdAt
    updatedAt
  }
}
